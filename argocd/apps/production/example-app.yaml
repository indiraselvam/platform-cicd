apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sample-app-production
  namespace: argocd
  labels:
    environment: production
spec:
  project: production
  source:
    repoURL: https://github.com/indiraselvam/platform-cicd.git
    targetRevision: main
    path: charts/example-app
    helm:
      valueFiles:
        - values.yaml
        - values-production.yaml
      # image.tag is bumped by the app repo's CI PR — nothing else in
      # values-production.yaml changes on a routine deploy.
  destination:
    server: https://kubernetes.default.svc
    namespace: sample-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 3
      backoff:
        duration: 15s
        factor: 2
        maxDuration: 3m
